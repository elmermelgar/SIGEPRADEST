{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoCurso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idtc</th>
                <th>Nombretc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipocurso_show', { 'id': entity.idTc }) }}">{{ entity.idTc }}</a></td>
                <td>{{ entity.nombreTc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipocurso_show', { 'id': entity.idTc }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipocurso_edit', { 'id': entity.idTc }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipocurso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
