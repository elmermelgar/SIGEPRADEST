{% extends 'base2.html.twig' %}
{% block active %}active-menu{% endblock %}
{% block Header %}
    <span style="color:royalblue;margin-left: 20px;font-size: 14px;">Bienvenido!</span>
{% endblock %}
{% block body %}
    <div class="col-md-12">
        <ol class="breadcrumb">
            <li class="active"><a href="{{ path('index_interesado') }}">Inicio</a></li>

        </ol>

        <div class="row">
            {% for c in cursos %}
                <div class="col-md-6">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            {{ c.nombreCurso }}
                        </div>

                        <div class="panel-body">
                            <div class="col-md-4">
                                {{ c.broshureInformativo }}
                            </div>
                            <div class="col-md-8">
                                <p>Cupo: {{ c.cantAlumnosLimit }}</p>
                                <br/>
                                <p>
                                    {{ c.textoInformativo }}
                                </p>
                            </div>
                        </div>
                        <div class="panel-footer">
                            <a href="{{ path('interesado_solicitud',{'idc' : c.idCurso}) }}">Llenar solicitud</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}