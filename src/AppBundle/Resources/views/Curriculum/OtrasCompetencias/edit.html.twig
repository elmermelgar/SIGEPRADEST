{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OtrasCompetencias edit</h1>

    {#{{ form(edit_form) }}#}


    {{ form_start(edit_form) }}
    {{form_widget(edit_form.idCurriculum, {attr :{"style":"display:none;"}}) }}
    {{ form_end(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('secretaria_otrascompetencias',{ 'cur': curriculum }) }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
